apiVersion: v1
data:
  JINA_LOG_LEVEL: INFO
  pythonunbuffered: '1'
  worker_class: uvicorn.workers.UvicornH11Worker
kind: ConfigMap
metadata:
  name: yolodetector-configmap
  namespace: default
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: yolodetector
  name: yolodetector
  namespace: default
spec:
  ports:
  - name: port
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: yolodetector
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolodetector
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: yolodetector
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        app: yolodetector
        jina_deployment_name: YOLODetector
        ns: default
        pod_type: WORKER
        shard_id: '0'
    spec:
      containers:
      - args:
        - executor
        - --name
        - YOLODetector
        - --extra-search-paths
        - ''
        - --k8s-namespace
        - default
        - --replicas
        - '3'
        - --uses
        - config.yml
        - --port
        - '8080'
        - --port-monitoring
        - '9090'
        - --uses-metas
        - '{}'
        - --uses-with
        - '{"weights_or_url": "$YOLO_WEIGHTS", "device": 0, "image_size": 640}'
        - --native
        command:
        - jina
        env:
        - name: POD_UID
          valueFrom:
            fieldRef:
              fieldPath: metadata.uid
        - name: JINA_DEPLOYMENT_NAME
          value: YOLODetector
        - name: K8S_DEPLOYMENT_NAME
          value: yolodetector
        - name: K8S_NAMESPACE_NAME
          value: default
        - name: K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        envFrom:
        - configMapRef:
            name: yolodetector-configmap
        image: jina-yolov5:1.0
        imagePullPolicy: IfNotPresent
        name: executor
        ports:
        - containerPort: 8080
        readinessProbe:
          exec:
            command:
            - jina
            - ping
            - executor
            - 127.0.0.1:8080
          initialDelaySeconds: 5
          periodSeconds: 20
          timeoutSeconds: 10
